import java.util.Scanner;

public class App {
    public static void main(String[] args) throws Exception {
        Engine fox = new Engine();
        Season s1 = new Season();
        Scanner sc = new Scanner(System.in);

        s1.addTeamsArray(fox.createTeams());

        s1.homeOrVisitor();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();
        s1.startRound();


        s1.table();
    

        
    }
}
